# Replicate Yarn 1's lenient peer dependency resolution.
# Yarn 1 never enforced peer dependencies; npm 7+ does by default.
# This is needed because some packages declare peer dependencies on older
# TypeScript versions that conflict with ours.
legacy-peer-deps=true

