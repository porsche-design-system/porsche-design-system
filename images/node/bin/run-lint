#!/usr/bin/env bash

set -o errexit
set -o pipefail

node_modules/.bin/concurrently \
  -n "storefront:lint,workshop:lint" \
  -c "cyan,magenta" \
  "yarn --cwd ./packages/storefront lint" \
  "yarn --cwd ./packages/workshop lint"
