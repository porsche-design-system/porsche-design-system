#!/usr/bin/env bash

set -o errexit
set -o pipefail

node_modules/.bin/concurrently \
  -n "TS,SCSS" \
  -c "cyan,yellow" \
  "yarn --cwd ./packages/workshop lint:ts" \
  "yarn --cwd ./packages/workshop lint:scss"
