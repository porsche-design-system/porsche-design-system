#!/usr/bin/env bash

set -o errexit
set -o pipefail

node_modules/.bin/concurrently \
  -k \
  -s "first" \
  -n "APP,VRT" \
  -c "blue,magenta" \
  "yarn --cwd ./packages/storefront start" \
  "node_modules/.bin/wait-on -t 120000 http-get://localhost:3000 && yarn --cwd ./packages/workshop vrt"
