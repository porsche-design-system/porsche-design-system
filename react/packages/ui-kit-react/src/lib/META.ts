const TYPES = {
    ATOM: "atom",
    MOLECULE: "molecule",
    ORGANISM: "organism",
    SCREEN: "screen",
    STRUCTURE: "structure"
}

const TYPE_ORDER = [TYPES.ATOM, TYPES.MOLECULE, TYPES.ORGANISM, TYPES.STRUCTURE, TYPES.SCREEN]

export const META = {
    TYPES,
    TYPE_ORDER
}
