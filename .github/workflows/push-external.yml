on:
  pull_request:
    branches:
      - main
name: 'Porsche Design System'
jobs:
  build-and-test:
    name: 'Build and Test'
    uses: ./.github/workflows/build-and-test.yml
    if: github.repository_owner != 'porsche-design-system'
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
