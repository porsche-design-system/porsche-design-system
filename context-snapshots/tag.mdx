# tag

## accessibility/page.mdx


export const metadata = {
  title: 'Tag: Accessibility - Porsche Design System',
  description: 'Accessibility information and best practices for the Tag component',
};

# Tag (Component: p-tag)

## Accessibility support

This component does not include any special accessibility features.


---

## api/page.mdx


export const metadata = {
  title: 'Tag - Porsche Design System',
  description: 'Detailed API reference for the Tag component, including properties, slots, events, and customizable CSS properties.'
}

# Tag (Component: p-tag)

<!-- Component API documentation -->

---

## configurator/page.mdx


**Code Example: tagStory**

Vanilla JS:
```html
<!doctype html>
<html lang="en" class="light">
<head>
  <title></title>
</head>
<body class="bg-canvas">

<p-tag>
  Some label
</p-tag>
<script>

</script>
</body>
</html>
```

React:
```jsx
import React from 'react';
import { PTag } from '@porsche-design-system/components-react';

export const Example = () => {
  return (
    <>
      <PTag>
        Some label
      </PTag>
    </>
  )
}
```

Angular:
```typescript
import { ChangeDetectionStrategy, Component } from '@angular/core';
import { PorscheDesignSystemModule } from '@porsche-design-system/components-angular';

@Component({
  selector: 'porsche-design-system-app',
  template: `
      <p-tag>
        Some label
      </p-tag>
  `,
  changeDetection: ChangeDetectionStrategy.OnPush,
  standalone: true,
  imports: [PorscheDesignSystemModule], // <-- PDS module is imported here
})
export class ExampleComponent {}
```

Vue:
```vue
<script setup lang="ts">
  import { PTag } from '@porsche-design-system/components-vue';
</script>

<template>
  <PTag>
    Some label
  </PTag>
</template>
```



export const metadata = {
  title: 'Tag - Porsche Design System',
  description: 'Learn how to use the Porsche Design System\'s Tag component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.'
}

# Tag (Component: p-tag)

`p-tag` is used to label, categorize, or organize items by using keywords that describe them.

<!-- Interactive Configurator component (code examples shown above) -->



---

## examples/page.mdx


**Code Example: tagStory**

Vanilla JS:
```html
<!doctype html>
<html lang="en" class="light">
<head>
  <title></title>
</head>
<body class="bg-canvas">

<p-tag>
  Some label
</p-tag>
<script>

</script>
</body>
</html>
```

React:
```jsx
import React from 'react';
import { PTag } from '@porsche-design-system/components-react';

export const Example = () => {
  return (
    <>
      <PTag>
        Some label
      </PTag>
    </>
  )
}
```

Angular:
```typescript
import { ChangeDetectionStrategy, Component } from '@angular/core';
import { PorscheDesignSystemModule } from '@porsche-design-system/components-angular';

@Component({
  selector: 'porsche-design-system-app',
  template: `
      <p-tag>
        Some label
      </p-tag>
  `,
  changeDetection: ChangeDetectionStrategy.OnPush,
  standalone: true,
  imports: [PorscheDesignSystemModule], // <-- PDS module is imported here
})
export class ExampleComponent {}
```

Vue:
```vue
<script setup lang="ts">
  import { PTag } from '@porsche-design-system/components-vue';
</script>

<template>
  <PTag>
    Some label
  </PTag>
</template>
```



export const metadata = {
  title: 'Tag - Porsche Design System',
  description: 'Discover detailed examples and insights of the Tag component.',
};

# Tag (Component: p-tag)

<!-- Table of Contents -->

## With slotted button

It is possible to add a `<button>` tag into the `p-tag` component. If you do this, the entire component becomes
clickable and no other content outside the button or link is allowed.

<!-- Component Story example (code examples shown above) -->

## With slotted link

It is possible to add `<a>` tag into the `p-tag` component. If you do this, the entire component becomes clickable and
no other content outside the button or link is allowed.

<!-- Component Story example (code examples shown above) -->

## Multiline

The contents of the `p-tag` component are rendered with `white-space: nowrap` by default. If multiline rendering is
needed (e.g. due to less amount of space), the default behaviour can be overwritten with CSS from outside.

<!-- Component Story example (code examples shown above) -->


---

## usage/page.mdx


export const metadata = {
  title: 'Tag - Porsche Design System',
  description: 'Learn how to properly use the Tag component with best practices and guidelines for effective design and user experience.'
}

# Tag (Component: p-tag)

<!-- Table of Contents -->

## Usage

Use tags to label, categorize, or organize items using keywords that describe them.

The following segment provides instructions for designers and developers regarding the appropriate utilization of this
component in various situations.

### Do:

- Use to indicate or highlight a certain attribute of an item.
- Use to show that content is mapped to one or multiple categories.
- Use to indicate that a certain filter is active.
- Use short labeling for easy scanning.
- Have a large number of tags horizontally scrollable, swipeable, or navigable with arrows.
- Have tags in a predefined space and move to the next line once they meet the boundary.
- Use color themes such as success, neutral, warning, and error for better visual perception
- Include icons to improve the faster perception.
- Add a link for further explanation about the tag for better understandability (With modal).
- Add a link for additional information that is not necessary for the task completion (With modal).

### Don't:

- Don't use more than two words (only if necessary).
- Don't implement something else than a link or button.
- Don't use Tags for navigation.


---

