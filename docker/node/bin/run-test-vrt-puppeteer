#!/usr/bin/env bash

set -o errexit
set -o pipefail

if
  [[ "--components-js" == "${1}" ]]
then
  PUPPETEER_EXECUTABLE_PATH=/ms-playwright/chromium-1055/chrome-linux/chrome yarn test:vrt:puppeteer:${1:2}
fi
