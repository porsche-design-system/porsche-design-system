import { ExampleVrt } from "../../../src/components/example/ExampleVrt"
import { Text } from "@porscheui/porsche-ui-kit"

<ExampleVrt>
  {() => {
    return (
      <React.Fragment>
        <Text type="large-title" as="h1">
          Large Title
        </Text>
      </React.Fragment>
    )
  }}
</ExampleVrt>

<ExampleVrt theme="inverted">
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Text type="large-title" as="h1" inverted={setTheme}>
          Large Title
        </Text>
      </React.Fragment>
    )
  }}
</ExampleVrt>
