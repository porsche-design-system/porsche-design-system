import { ExampleVrt } from "../../../src/components/example/ExampleVrt"
import { Text } from "@porscheui/porsche-ui-kit"

<ExampleVrt>
  {() => {
    return (
      <React.Fragment>
        <Text type="copy">
          Copy text with <strong>strong text</strong> and <b>bold text</b>
        </Text>
      </React.Fragment>
    )
  }}
</ExampleVrt>

<ExampleVrt theme="inverted">
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Text type="small" inverted={setTheme}>
          Small text with <strong>strong text</strong> and <b>bold text</b>
        </Text>
      </React.Fragment>
    )
  }}
</ExampleVrt>
