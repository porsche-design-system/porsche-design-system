import { HashRouter as Router, Link } from "react-router-dom";
import { Example } from "../../../src/components/example/Example"
import { ButtonRegular } from "@porscheui/porsche-ui-kit"

# Button Regular

## Introduction
All button types can be generated out of the basic button by implementing additional modifier classes or React proprties.

### Button regular basic (default)
If using basic type, no extra class/prop declaration is needed.

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular inverted={setTheme}>Click Here!</ButtonRegular>
        <ButtonRegular inverted={setTheme} disabled>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

### Button regular ghost

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular type="ghost" inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

### Button regular sales

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular type="sales" inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

### Button regular sales-ghost

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular type="sales-ghost" inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

### Button regular highlight

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular type="highlight" inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

---

### Button regular with specific icon
If another icon needs to be implemented, just replace the default icon with your new icon.

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular icon="icon_phone.min.svg" inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

---

### Button regular loader state
If a button has a loading state, it must also be set in a disabled state to prevent click behaviour caused by the user.
To show the loader icon, the default svg icon must be replaced with the loader component

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular loading inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>

---

### Button as link
Buttons can also be rendered as link elements if used as a navigation item.

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <Router>
        <ButtonRegular as={Link} {...{ to: "/lorem/ipsum" }} inverted={setTheme}>Click Here!</ButtonRegular>
        <ButtonRegular as={Link} {...{ to: "" }} loading inverted={setTheme}>Click Here!</ButtonRegular>
      </Router>
    )
}}
</Example>


### Button regular basic TEST with new compiled icon
If using basic type, no extra class/prop declaration is needed.

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <ButtonRegular iconPath="https://video.porsche.com/0.0.2/icon/" icon="icon_arrow-left-new.min.svg" inverted={setTheme}>Click Here!</ButtonRegular>
      </React.Fragment>
    )
}}
</Example>
