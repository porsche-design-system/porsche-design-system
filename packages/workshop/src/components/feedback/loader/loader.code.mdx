import { Example } from "../../../src/components/example/Example"
import { Loader } from "@porscheui/porsche-ui-kit"

# Loader

## Introduction
TBD

### Loader standard

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Loader inverted={setTheme} />
      </React.Fragment>
    )
}}
</Example>

### Loader sizes

#### x-small

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Loader size="x-small" inverted={setTheme} />
      </React.Fragment>
    )
}}
</Example>

#### small

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Loader size="small" inverted={setTheme} />
      </React.Fragment>
    )
}}
</Example>

#### medium

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Loader size="medium" inverted={setTheme} />
      </React.Fragment>
    )
}}
</Example>

#### large

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Loader size="large" inverted={setTheme} />
      </React.Fragment>
    )
}}
</Example>

#### x-large

<Example noTheme={false}>
  {(theme) => {
    const setTheme = theme === "inverted" ? true : false
    return (
      <React.Fragment>
        <Loader size="x-large" inverted={setTheme} />
      </React.Fragment>
    )
}}
</Example>
