import { palette } from '../../palette';

/** Holds the **frosted-strong** color (light), typically used as a stronger frosted background. */
export const colorFrostedStrongLight = palette.light.grey['200a'];
