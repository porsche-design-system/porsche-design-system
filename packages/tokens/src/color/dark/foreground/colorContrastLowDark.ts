import { palette } from '../../palette';

/** Holds the **contrast-low** color (dark), intended only for decorative elements, as it is not accessibility-compliant. */
export const colorContrastLowDark = palette.dark.grey['500a'];
