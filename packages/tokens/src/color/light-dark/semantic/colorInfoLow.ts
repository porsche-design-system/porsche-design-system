import { colorInfoLowDark } from '../../dark';
import { colorInfoLowLight } from '../../light';

/** Holds the **info-low** color, typically used for text or border. */
export const colorInfoLow = `light-dark(${colorInfoLowLight},${colorInfoLowDark})`;
