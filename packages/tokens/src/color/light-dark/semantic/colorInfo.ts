import { colorInfoDark } from '../../dark';
import { colorInfoLight } from '../../light';

/** Holds the **info** color, typically used for text. */
export const colorInfo = `light-dark(${colorInfoLight},${colorInfoDark})`;
