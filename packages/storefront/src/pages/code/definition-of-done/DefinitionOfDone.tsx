import React from "react"
import {Markdown} from "../../../components/markdown/Markdown"

export const DefinitionOfDone: React.FunctionComponent = () => {
  return <Markdown path={require('./definition-of-done.md')}/>
}
