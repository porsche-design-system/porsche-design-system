import React from "react"
import { Markdown } from "../../../components/markdown/Markdown"

export const Installation0x: React.FunctionComponent = () => {
  return <Markdown path={require("./installation-0.x.md")} />
}
