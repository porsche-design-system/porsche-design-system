import React from "react"
import { Markdown } from "../../../components/markdown/Markdown"

export const InstallationV0: React.FunctionComponent = () => {
  return <Markdown path={require("./installation-v0.md")} />
}
