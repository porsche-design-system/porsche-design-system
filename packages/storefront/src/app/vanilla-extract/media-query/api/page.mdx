import { StylesTable } from '@/components/common/StylesTable';
import { TableOfContents } from '@/components/common/TableOfContents';
import { filterMeta } from '@/lib/filterMeta';
import { vanillaExtractMeta } from '@porsche-design-system/vanilla-extract-meta';

export const metadata = {
  title: 'Vanilla Extract: Media Query - Porsche Design System',
  description: 'Detailed API reference for the Media Query style.',
};

# Media Query

<TableOfContents headings={['JS Functions', 'JS']} />

## JS Functions

<StylesTable meta={vanillaExtractMeta.mediaQuery.helpers} columnLabel="JS Function" />

## JS

<StylesTable meta={filterMeta(vanillaExtractMeta.mediaQuery, (k) => k !== 'helpers')} columnLabel="JS" />
