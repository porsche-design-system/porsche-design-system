import { StylesTable } from '@/components/common/StylesTable';
import { TableOfContents } from '@/components/common/TableOfContents';
import { filterMeta } from '@/lib/filterMeta';
import { vanillaExtractMeta } from '@porsche-design-system/vanilla-extract-meta';

export const metadata = {
  title: 'Vanilla Extract: Skeleton - Porsche Design System',
  description: 'Detailed API reference for the skeleton style.',
};

# Skeleton

<TableOfContents headings={['JS Functions', 'JS']} />

## JS Functions

<StylesTable meta={vanillaExtractMeta.skeleton.helpers} columnLabel="JS Function" />

## JS

<StylesTable meta={filterMeta(vanillaExtractMeta.skeleton, (k) => k !== 'helpers')} columnLabel="JS" />
