import { ComponentStory } from '@/components/playground/ComponentStory';
import { gradientVanillaExtractStory } from '@/app/vanilla-extract/gradient/examples/gradient.stories';

export const metadata = {
  title: 'Vanilla Extract: Gradient - Porsche Design System',
  description: 'Learn about the Vanilla Extract styles of our design language and how to use them.',
};

# Gradient

The Porsche Design System provides a JS Variable for the gradient style to be used in Vanilla Extract.

Styles import:
`import { gradientStopsFadeDark } from '@porsche-design-system/components-{js|angular|react|vue}/vanilla-extract';`

<ComponentStory story={gradientVanillaExtractStory} backgroundColor="surface" showCodeBlock={false} />
