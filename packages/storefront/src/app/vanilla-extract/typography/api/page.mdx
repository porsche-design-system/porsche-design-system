import { StylesTable } from '@/components/common/StylesTable';
import { filterMeta } from '@/lib/filterMeta';
import { withoutDeprecated } from '@/lib/withoutDeprecated';
import { TableOfContents } from '@/components/common/TableOfContents';
import { vanillaExtractMeta } from '@porsche-design-system/vanilla-extract-meta';

export const metadata = {
  title: 'Vanilla Extract: Typography - Porsche Design System',
  description: 'Detailed API reference for the typography style.',
};

# Typography

<TableOfContents headings={['JS Objects', 'JS Variables']} />

## JS Objects

### Heading

<StylesTable
  meta={filterMeta(withoutDeprecated(vanillaExtractMeta.typography), (k) => k.startsWith('proseHeading'))}
  columnLabel="JS"
/>

### Text

<StylesTable
  meta={filterMeta(withoutDeprecated(vanillaExtractMeta.typography), (k) => k.startsWith('proseText'))}
  columnLabel="JS"
/>

## JS Variables

<StylesTable
  meta={{
    ...vanillaExtractMeta.font.family,
    ...vanillaExtractMeta.font.weight,
    ...vanillaExtractMeta.font.lineHeight,
    ...vanillaExtractMeta.font.size,
  }}
  columnLabel="JS"
/>
