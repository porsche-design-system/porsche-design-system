import { StylesTable } from '@/components/common/StylesTable';
import { filterMeta } from '@/lib/filterMeta';
import { TableOfContents } from '@/components/common/TableOfContents';
import { vanillaExtractMeta } from '@porsche-design-system/vanilla-extract-meta';

export const metadata = {
  title: 'Vanilla Extract: Color - Porsche Design System',
  description: 'Detailed API reference for the Color style.',
};

# Color

<TableOfContents headings={['JS']} />

## JS

### Background

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => k.startsWith('colorCanvas') || k.startsWith('colorSurface') || k.startsWith('colorFrosted') || k.startsWith('colorBackdrop'))}
  columnLabel="JS"
  showColorSwatch
/>

### Foreground

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => k.startsWith('colorPrimary') || k.startsWith('colorContrast'))}
  columnLabel="JS"
  showColorSwatch
/>

### Semantic

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => k.startsWith('colorInfo') || k.startsWith('colorSuccess') || k.startsWith('colorWarning') || k.startsWith('colorError'))}
  columnLabel="JS"
  showColorSwatch
/>

### A11y

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => k.startsWith('colorFocus'))}
  columnLabel="JS"
  showColorSwatch
/>
