import { TableOfContents } from '@/components/common/TableOfContents';
import { mediaQueryStory } from '@/app/tailwind/media-query/examples/media-query.stories';
import { ComponentStory } from '@/components/playground/ComponentStory';

export const metadata = {
  title: 'Tailwind: Media Query - Porsche Design System',
  description: 'Learn about the Tailwind theme of our design language and how to use it.',
};

# Media Query

The Porsche Design System Tailwind theme customizes the `--breakpoint-*` CSS variables, which define the responsive
breakpoints used by Tailwind’s `xs` (480px), `sm` (760px), `md` (1000px), `lg` (1300px), `xl` (1760px), and `2xl`
(1920px) classes. These breakpoints can be applied using utilities like `sm:*`, `max-sm:*`, or range queries such as
`sm:max-md:*`.

<ComponentStory story={mediaQueryStory} />
