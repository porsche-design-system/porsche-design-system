import { TableOfContents } from '@/components/common/TableOfContents';

export const metadata = {
  title: 'Tailwind: Blur - Porsche Design System',
  description: 'Detailed API reference for the Blur style.',
};

# Blur

<TableOfContents headings={[]} />

| Tailwind CSS variable | Tailwind CSS class                        | Description                                                                                                      |
| --------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `--blur-frosted`      | `.backdrop-blur-frosted`, `.blur-frosted` | Applies a **frosted** effect combined with a semi-transparent color using `backdrop-filter` or `filter: blur()`. |
