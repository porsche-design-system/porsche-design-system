import { ComponentStory } from '@/components/playground/ComponentStory';
import { gradientStory } from '@/app/tailwind/gradient/examples/gradient.stories';

export const metadata = {
  title: 'Tailwind: Gradient - Porsche Design System',
  description: 'Learn about the Tailwind theme of our design language and how to use it.',
};

# Gradient

The Porsche Design System Tailwind theme adds variables for different gradients `--gradient-to-*`, which can be used in
Tailwind classes like `.bg-linear-*`, `.bg-radial-*` and others.

<ComponentStory story={gradientStory} />
