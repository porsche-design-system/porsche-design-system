import { TableOfContents } from '@/components/common/TableOfContents';
import { ComponentStory } from '@/components/playground/ComponentStory';
import {
  typographyStoryHeading,
  typographyStoryText,
} from '@/app/(main)/tailwindcss/typography/examples/typography.stories';

export const metadata = {
  title: 'Tailwind CSS: Typography - Porsche Design System',
  description: 'Learn about the Tailwind typography of our design language and how to use it.',
};

# Typography

<TableOfContents headings={['Heading', 'Text']} />

The Porsche Design System Tailwind theme includes the custom `@utility` CSS classes `.prose-display-*`,
`.prose-heading-*` and `.prose-text-*` to style headings and paragraphs consistently.

Additionally, it customizes Tailwind variables such as `--font-*`, `--leading-*` and `--text-*`, which affect multiple
Tailwind classes including `.font-*`, `.leading-*`, `.text-*` and others.

## Heading

<ComponentStory story={typographyStoryHeading} />

## Text

<ComponentStory story={typographyStoryText} />
