import { TableOfContents } from '@/components/common/TableOfContents';
import { Code } from '@/components/common/Code';
import { ScssVariablesTable } from '@/components/common/ScssVariablesTable';
import { scssMeta } from '@porsche-design-system/scss';

export const metadata = {
  title: 'SCSS: Typography - Porsche Design System',
  description: 'Detailed API reference for the typography style.',
};

# Typography

<TableOfContents headings={['SCSS Mixins', 'SCSS Variables', 'CJK auto-detection']} />

## SCSS Mixins

### Heading

| SCSS Mixin                                             | Description                                                                                                           |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| <Code variant="nowrap">@mixin prose-heading-5xl</Code> | Applies the **5x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <Code variant="nowrap">@mixin prose-heading-4xl</Code> | Applies the **4x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <Code variant="nowrap">@mixin prose-heading-3xl</Code> | Applies the **3x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <Code variant="nowrap">@mixin prose-heading-2xl</Code> | Applies the **2x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <Code variant="nowrap">@mixin prose-heading-xl</Code>  | Applies the **x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.  |
| <Code variant="nowrap">@mixin prose-heading-lg</Code>  | Applies the **large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.    |
| <Code variant="nowrap">@mixin prose-heading-md</Code>  | Applies the **medium** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.   |
| <Code variant="nowrap">@mixin prose-heading-sm</Code>  | Applies the **small** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.    |
| <Code variant="nowrap">@mixin prose-heading-xs</Code>  | Applies the **x-small** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.  |
| <Code variant="nowrap">@mixin prose-heading-2xs</Code> | Applies the **2x-small** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |

### Text

| SCSS Mixin                                          | Description                                                                                               |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| <Code variant="nowrap">@mixin prose-text-5xl</Code> | Applies the **5x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |
| <Code variant="nowrap">@mixin prose-text-4xl</Code> | Applies the **4x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |
| <Code variant="nowrap">@mixin prose-text-3xl</Code> | Applies the **3x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |
| <Code variant="nowrap">@mixin prose-text-2xl</Code> | Applies the **2x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |
| <Code variant="nowrap">@mixin prose-text-xl</Code>  | Applies the **x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.  |
| <Code variant="nowrap">@mixin prose-text-lg</Code>  | Applies the **large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.    |
| <Code variant="nowrap">@mixin prose-text-md</Code>  | Applies the **medium** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.   |
| <Code variant="nowrap">@mixin prose-text-sm</Code>  | Applies the **small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.    |
| <Code variant="nowrap">@mixin prose-text-xs</Code>  | Applies the **x-small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.  |
| <Code variant="nowrap">@mixin prose-text-2xs</Code> | Applies the **2x-small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |

## SCSS Variables

### Family

<ScssVariablesTable variables={scssMeta.theme.typography.family} />

### Weight

<ScssVariablesTable variables={scssMeta.theme.typography.weight} />

### Line height

<ScssVariablesTable variables={scssMeta.theme.typography.lineHeight} />

### Text

<ScssVariablesTable variables={scssMeta.theme.typography.text} />

## CJK auto-detection

Use the `cjk-font-family()` mixin on any rule that sets `font-family` to automatically swap to the locale-specific
**Porsche Next** font stack for **Simplified Chinese**, **Traditional Chinese**, **Japanese** or **Korean** based on the
nearest `lang` attribute (typically set on `<html>`).

| SCSS Mixin                                             | Description                                                                                                                                                                     |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <Code variant="nowrap">@mixin cjk-font-family()</Code> | Applies locale-specific **Porsche Next** font stacks for **CJK** languages (Simplified Chinese, Traditional Chinese, Japanese, Korean) based on the element's `lang` attribute. |

```scss
@use '@porsche-design-system/components-{js|angular|react|vue}/scss' as *;

.headline {
  font-family: $font-porsche-next;
  @include cjk-font-family;
}
```

The `prose-heading-*()` and `prose-text-*()` mixins already include this `:lang()` logic, so when you compose styles
with them you can skip the extra `@include`. Reach for the per-language tokens (`$font-porsche-next-zh-hans`, …) only
when you intentionally need to pin a specific CJK family regardless of the document's language.
