import { TableOfContents } from '@/components/common/TableOfContents';
import { ScssVariablesTable } from '@/components/common/ScssVariablesTable';
import { ScssMixinsTable } from '@/components/common/ScssMixinsTable';
import { scssMeta, kindOf, flatten } from '@porsche-design-system/scss';

export const metadata = {
  title: 'SCSS: Grid - Porsche Design System',
  description: 'Detailed API reference for the Grid style.',
};

# Grid

<TableOfContents headings={['SCSS Mixins', 'SCSS Variables']} />

## SCSS Mixins

<ScssMixinsTable mixins={flatten(scssMeta.grid).filter((node) => kindOf(node) === 'utility')} />

## SCSS Variables

### Base

<ScssVariablesTable variables={flatten(scssMeta.grid.gap)} />

### Narrow

<ScssVariablesTable variables={flatten(scssMeta.grid.narrow)} />

### Basic

<ScssVariablesTable variables={flatten(scssMeta.grid.basic)} />

### Extended

<ScssVariablesTable variables={flatten(scssMeta.grid.extended)} />

### Wide

<ScssVariablesTable variables={flatten(scssMeta.grid.wide)} />

### Full

<ScssVariablesTable variables={flatten(scssMeta.grid.full)} />
