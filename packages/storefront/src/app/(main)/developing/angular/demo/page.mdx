import { TableOfContents } from '@/components/common/TableOfContents';
import { WebsiteViewer } from '@/components/common/WebsiteViewer';

export const metadata = {
  title: 'Angular: Demo - Porsche Design System',
  description: 'Start developing in Angular with the Porsche Design System.',
};

# Angular

<TableOfContents headings={[]} />

We've created a public GitHub repository with a basic sample project to demonstrate how Angular is managed in real-world
code. Explore the Angular example project here:

<WebsiteViewer
  sourceCodePath="frameworks/angular"
  viewPath="angular"
  title="Angular: Demo application"
/>
