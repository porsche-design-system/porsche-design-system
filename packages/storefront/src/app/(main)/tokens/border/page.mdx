import { TableOfContents } from '@/components/common/TableOfContents';
import { TokensTable } from '@/components/common/TokensTable';
import { tokensMeta } from '@porsche-design-system/tokens-meta';

export const metadata = {
  title: 'Tokens: Border - Porsche Design System',
  description: 'Detailed API reference for the border tokens.',
};

# Border

Defines the rounding of interactive controls, containers, and pill shapes. The visual language is primarily shaped by
**radiusXl** (`12px`) for controls and **radius3Xl** (`24px`) for tiles — establishing a consistent, modern appearance
across all touchpoints.

<TableOfContents headings={[]} />

## Tokens

### Radius

<TokensTable meta={tokensMeta.border.radius} />
