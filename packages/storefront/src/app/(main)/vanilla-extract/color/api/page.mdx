import { vanillaExtractMeta } from '@porsche-design-system/vanilla-extract';
import { TableOfContents } from '@/components/common/TableOfContents';
import { StylesTable } from '@/components/common/StylesTable';
import { filterMeta } from '@/lib/filterMeta';

export const metadata = {
  title: 'Vanilla Extract: Color - Porsche Design System',
  description: 'Detailed API reference for the Color style.',
};

# Color

<TableOfContents headings={['JS']} />

## JS

### Background

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => /^color(Canvas|Surface|Frosted|Backdrop)/.test(k))}
  columnLabel="JS"
  showColorSwatch
/>

### Foreground

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => /^color(Primary|Contrast)/.test(k))}
  columnLabel="JS"
  showColorSwatch
/>

### Semantic

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => /^color(Info|Success|Warning|Error)/.test(k))}
  columnLabel="JS"
  showColorSwatch
/>

### A11y

<StylesTable
  meta={filterMeta(vanillaExtractMeta.color.lightDark, (k) => /^colorFocus/.test(k))}
  columnLabel="JS"
  showColorSwatch
/>
