Use the `aria` property to provide an accessible name and optional description for the tablist when the component is
used as a tabbed interface with `<button>` children. The property is not applied when using `<a>` tags as a link list.
