If the `p-tabs-bar` is used as a link list (navigation context), the semantic of that UI component changes completely -
from a **tabbed interface** where content changes happen **on-site** to a **list of links** where content changes are
**route-based**. 