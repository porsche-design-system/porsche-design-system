import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { switchMeta } from '@/app/(main)/components/switch/switch.meta';

export const metadata = {
  title: 'Switch - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Switch component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Switch <ComponentStatus tagName="p-switch"></ComponentStatus>

<switchMeta.introduction />

<Configurator tagName="p-switch" story={switchMeta.configurator.story} />
