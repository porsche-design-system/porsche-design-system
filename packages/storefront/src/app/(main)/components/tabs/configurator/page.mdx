import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { tabsMeta } from '@/app/(main)/components/tabs/tabs.meta';

export const metadata = {
  title: 'Tabs - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Tabs component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Tabs <ComponentStatus tagName="p-tabs"></ComponentStatus>

<tabsMeta.introduction />

<Configurator tagName="p-tabs" story={tabsMeta.configurator.story} />
