Instead of using the the property, the table's caption can be provided via the `caption` slot. Its content is rendered
visually while giving you full control over its appearance, and is also exposed to assistive technologies.
