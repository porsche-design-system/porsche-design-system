import { ComponentStatus } from '@/components/components/ComponentStatus';
import { ComponentApi } from "@/components/components/ComponentApi";
import { paginationMeta } from '@/app/(main)/components/pagination/pagination.meta';

export const metadata = {
  title: 'Pagination - Porsche Design System',
  description: 'Detailed API reference for the Pagination component, including properties, slots, events, and customizable CSS properties.'
}

# Pagination <ComponentStatus tagName="p-pagination"></ComponentStatus>

<ComponentApi api={paginationMeta.api} />