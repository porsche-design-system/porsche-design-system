The `color` property can be set to `inherit` to derive the icon's color from the `currentcolor` of a parent element
(e.g. a `p-text`). This ensures the icon matches the surrounding text color automatically.

