import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { inputUrlMeta } from '@/app/(main)/components/input-url/input-url.meta';

export const metadata = {
  title: 'Input URL - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Input URL component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Input URL <ComponentStatus tagName="p-input-url"></ComponentStatus>

<inputUrlMeta.introduction />

<Configurator tagName="p-input-url" story={inputUrlMeta.configurator.story} slotStories={inputUrlMeta.configurator.slotStories} />
