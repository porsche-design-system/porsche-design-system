import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { flyoutMeta } from '@/app/(main)/components/flyout/flyout.meta';

export const metadata = {
  title: 'Flyout - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Flyout component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Flyout <ComponentStatus tagName="p-flyout"></ComponentStatus>

<flyoutMeta.introduction />

<Configurator
  tagName="p-flyout"
  story={flyoutMeta.configurator.story}
  slotStories={flyoutMeta.configurator.slotStories}
/>

