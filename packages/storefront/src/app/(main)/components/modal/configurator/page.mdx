import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { modalMeta } from '@/app/(main)/components/modal/modal.meta';

export const metadata = {
  title: 'Modal - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Modal component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Modal <ComponentStatus tagName="p-modal"></ComponentStatus>

<modalMeta.introduction />

<Configurator
  tagName="p-modal"
  story={modalMeta.configurator.story}
  slotStories={modalMeta.configurator.slotStories}
/>
