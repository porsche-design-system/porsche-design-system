import { ComponentStatus } from '@/components/components/ComponentStatus';
import { ComponentApi } from "@/components/components/ComponentApi";
import { headingMeta } from '@/app/(main)/components/heading/heading.meta';

export const metadata = {
  title: 'Heading - Porsche Design System',
  description: 'Detailed API reference for the Heading component, including properties, slots, events, and customizable CSS properties.'
}

# Heading <ComponentStatus tagName="p-heading"></ComponentStatus>

<ComponentApi api={headingMeta.api} />