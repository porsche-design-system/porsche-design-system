import { TableOfContents } from '@/components/common/TableOfContents';
import {
  fontPorscheNext,
  fontWeightNormal,
  fontWeightSemibold,
  fontWeightBold,
  leadingNormal,
  typescale2Xs,
  typescaleXs,
  typescaleSm,
  typescaleMd,
  typescaleLg,
  typescaleXl,
  typescale2Xl,
  typescale3Xl,
  typescale4Xl,
  typescale5Xl,
} from '@porsche-design-system/tokens';

export const metadata = {
  title: 'SCSS: Typography - Porsche Design System',
  description: 'Detailed API reference for the typography style.',
};

# Typography

<TableOfContents headings={['SCSS Mixins', 'SCSS Variables']} />

## SCSS Mixins

### Heading

| SCSS Mixin                                                          | Description                                                                                                           |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| <code className="whitespace-nowrap">@mixin prose-heading-5xl</code> | Applies the **5x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <code className="whitespace-nowrap">@mixin prose-heading-4xl</code> | Applies the **4x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <code className="whitespace-nowrap">@mixin prose-heading-3xl</code> | Applies the **3x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <code className="whitespace-nowrap">@mixin prose-heading-2xl</code> | Applies the **2x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| <code className="whitespace-nowrap">@mixin prose-heading-xl</code>  | Applies the **x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.  |
| <code className="whitespace-nowrap">@mixin prose-heading-lg</code>  | Applies the **large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.    |
| <code className="whitespace-nowrap">@mixin prose-heading-md</code>  | Applies the **medium** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.   |
| <code className="whitespace-nowrap">@mixin prose-heading-sm</code>  | Applies the **small** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.    |

### Text

| SCSS Mixin                                                       | Description                                                                                               |
| ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| <code className="whitespace-nowrap">@mixin prose-text-xl</code>  | Applies the **x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.  |
| <code className="whitespace-nowrap">@mixin prose-text-lg</code>  | Applies the **large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.    |
| <code className="whitespace-nowrap">@mixin prose-text-md</code>  | Applies the **medium** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.   |
| <code className="whitespace-nowrap">@mixin prose-text-sm</code>  | Applies the **small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.    |
| <code className="whitespace-nowrap">@mixin prose-text-xs</code>  | Applies the **x-small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.  |
| <code className="whitespace-nowrap">@mixin prose-text-2xs</code> | Applies the **2x-small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |

## SCSS Variables

| SCSS Variable                                                    | Description                                                                               | Value                                                     |
| ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| <code className="whitespace-nowrap">$font-porsche-next</code>    | Holds the **Porsche Next** font family along with fallback fonts.                         | <code className="select-none">{fontPorscheNext}</code>    |
| <code className="whitespace-nowrap">$font-weight-normal</code>   | Holds the **normal** font weight optimized for the Porsche Next typeface.                 | <code className="select-none">{fontWeightNormal}</code>   |
| <code className="whitespace-nowrap">$font-weight-semibold</code> | Holds the **semibold** font weight optimized for the Porsche Next typeface.               | <code className="select-none">{fontWeightSemibold}</code> |
| <code className="whitespace-nowrap">$font-weight-bold</code>     | Holds the **bold** font weight optimized for the Porsche Next typeface.                   | <code className="select-none">{fontWeightBold}</code>     |
| <code className="whitespace-nowrap">$leading-normal</code>       | Holds a dynamic default line height specifically optimized for the Porsche Next typeface. | <code className="select-none">{leadingNormal}</code>      |
| <code className="whitespace-nowrap">$typescale-2xs</code>        | Holds the **2x-small** font size optimized for the Porsche Next typeface.                 | <code className="select-none">{typescale2Xs}</code>       |
| <code className="whitespace-nowrap">$typescale-xs</code>         | Holds the **x-small** font size optimized for the Porsche Next typeface.                  | <code className="select-none">{typescaleXs}</code>        |
| <code className="whitespace-nowrap">$typescale-sm</code>         | Holds the **small** font size optimized for the Porsche Next typeface.                    | <code className="select-none">{typescaleSm}</code>        |
| <code className="whitespace-nowrap">$typescale-md</code>         | Holds the **medium** font size optimized for the Porsche Next typeface.                   | <code className="select-none">{typescaleMd}</code>        |
| <code className="whitespace-nowrap">$typescale-lg</code>         | Holds the **large** font size optimized for the Porsche Next typeface.                    | <code className="select-none">{typescaleLg}</code>        |
| <code className="whitespace-nowrap">$typescale-xl</code>         | Holds the **x-large** font size optimized for the Porsche Next typeface.                  | <code className="select-none">{typescaleXl}</code>        |
| <code className="whitespace-nowrap">$typescale-2xl</code>        | Holds the **2x-large** font size optimized for the Porsche Next typeface.                 | <code className="select-none">{typescale2Xl}</code>       |
| <code className="whitespace-nowrap">$typescale-3xl</code>        | Holds the **3x-large** font size optimized for the Porsche Next typeface.                 | <code className="select-none">{typescale3Xl}</code>       |
| <code className="whitespace-nowrap">$typescale-4xl</code>        | Holds the **4x-large** font size optimized for the Porsche Next typeface.                 | <code className="select-none">{typescale4Xl}</code>       |
| <code className="whitespace-nowrap">$typescale-5xl</code>        | Holds the **5x-large** font size optimized for the Porsche Next typeface.                 | <code className="select-none">{typescale5Xl}</code>       |
