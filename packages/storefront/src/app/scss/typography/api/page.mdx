import { TableOfContents } from '@/components/common/TableOfContents';

export const metadata = {
  title: 'SCSS: Typography - Porsche Design System',
  description: 'Detailed API reference for the typography style.',
};

# Typography

<TableOfContents headings={['Classes', 'Variables']} />

## SCSS Mixins

### Display

| SCSS Mixin                | Description                                                                        |
| ------------------------- | ---------------------------------------------------------------------------------- |
| `@mixin prose-display-lg` | Applies the **large** variant of extra-large typography primarily to `<h1>` tags.  |
| `@mixin prose-display-md` | Applies the **medium** variant of extra-large typography primarily to `<h1>` tags. |
| `@mixin prose-display-sm` | Applies the **small** variant of extra-large typography primarily to `<h1>` tags.  |

### Heading

| SCSS Mixin                    | Description                                                                                                           |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `@mixin pds-heading-xx-large` | Applies the **2x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags. |
| `@mixin pds-heading-x-large`  | Applies the **x-large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.  |
| `@mixin pds-heading-large`    | Applies the **large** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.    |
| `@mixin pds-heading-medium`   | Applies the **medium** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.   |
| `@mixin pds-heading-small`    | Applies the **small** heading typography variant primarily to `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` tags.    |

### Text

| SCSS Mixin                 | Description                                                                                               |
| -------------------------- | --------------------------------------------------------------------------------------------------------- |
| `@mixin pds-text-x-large`  | Applies the **x-large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.  |
| `@mixin pds-text-large`    | Applies the **large** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.    |
| `@mixin pds-text-medium`   | Applies the **medium** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.   |
| `@mixin pds-text-small`    | Applies the **small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.    |
| `@mixin pds-text-x-small`  | Applies the **x-small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags.  |
| `@mixin pds-text-xx-small` | Applies the **2x-small** text typography variant primarily to `<p>`, `<ul>`, `<ol>`, `<blockquote>` tags. |
