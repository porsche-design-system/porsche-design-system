import { TableOfContents } from '@/components/common/TableOfContents';
import {
  radius3Xl,
  radius4Xl,
  radiusXl,
  radius2Xl,
  radiusFull,
  radiusLg,
  radiusMd,
  radiusSm,
  radiusXs,
} from '@porsche-design-system/tokens';

export const metadata = {
  title: 'SCSS: Border - Porsche Design System',
  description: 'Detailed API reference for the Border style.',
};

# Border

<TableOfContents headings={[]} />

## SCSS Variables

### Radius

| SCSS Variable                                           | Description                                | Value                                             |
| ------------------------------------------------------- | ------------------------------------------ | ------------------------------------------------- |
| <code className="whitespace-nowrap">$radius-xs</code>   | Holds a **x-small** `border-radius`.       | <code className="select-none">{radiusXs}</code>   |
| <code className="whitespace-nowrap">$radius-sm</code>   | Holds a **small** `border-radius`.         | <code className="select-none">{radiusSm}</code>   |
| <code className="whitespace-nowrap">$radius-md</code>   | Holds a **medium** `border-radius`.        | <code className="select-none">{radiusMd}</code>   |
| <code className="whitespace-nowrap">$radius-lg</code>   | Holds a **large** `border-radius`.         | <code className="select-none">{radiusLg}</code>   |
| <code className="whitespace-nowrap">$radius-xl</code>   | Holds a **x-large** `border-radius`.       | <code className="select-none">{radiusXl}</code>   |
| <code className="whitespace-nowrap">$radius-2xl</code>  | Holds a **2x-large** `border-radius`.      | <code className="select-none">{radius2Xl}</code>  |
| <code className="whitespace-nowrap">$radius-3xl</code>  | Holds a **3x-large** `border-radius`.      | <code className="select-none">{radius3Xl}</code>  |
| <code className="whitespace-nowrap">$radius-4xl</code>  | Holds a **4x-large** `border-radius`.      | <code className="select-none">{radius4Xl}</code>  |
| <code className="whitespace-nowrap">$radius-full</code> | Holds a **fully** rounded `border-radius`. | <code className="select-none">{radiusFull}</code> |
