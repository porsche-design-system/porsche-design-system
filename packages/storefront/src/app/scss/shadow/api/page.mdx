import { shadowLg, shadowMd, shadowSm } from '@porsche-design-system/tokens';
import { TableOfContents } from '@/components/common/TableOfContents';

export const metadata = {
  title: 'SCSS: Shadow - Porsche Design System',
  description: 'Detailed API reference for the Shadow style.',
};

# Shadow

<TableOfContents headings={[]} />

## SCSS Variables

| SCSS Variable                                         | Description                  | Value                                           |
| ----------------------------------------------------- | ---------------------------- | ----------------------------------------------- |
| <code className="whitespace-nowrap">$shadow-lg</code> | Holds a **large** `shadow`.  | <code className="select-none">{shadowSm}</code> |
| <code className="whitespace-nowrap">$shadow-md</code> | Holds a **medium** `shadow`. | <code className="select-none">{shadowMd}</code> |
| <code className="whitespace-nowrap">$shadow-sm</code> | Holds a **small** `shadow`.  | <code className="select-none">{shadowLg}</code> |
