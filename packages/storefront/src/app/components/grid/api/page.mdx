import { ComponentStatus } from '@/components/components/ComponentStatus';
import { ComponentApi } from "@/components/components/ComponentApi";

export const metadata = {
  title: 'Grid - Porsche Design System',
  description: 'Detailed API reference for the Grid component, including properties, slots, events, and customizable CSS properties.'
}

# Grid <ComponentStatus tagName="p-grid"></ComponentStatus>

<ComponentApi tagName="p-grid" />