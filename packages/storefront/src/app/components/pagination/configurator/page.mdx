import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from "@/components/playground/Configurator";
import { paginationStory } from "@/app/components/pagination/pagination.stories";

export const metadata = {
  title: 'Pagination - Porsche Design System',
  description: 'Learn how to use the Porsche Design System\'s Pagination component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.'
}

# Pagination <ComponentStatus tagName="p-pagination"></ComponentStatus>

To adapt the pagination to the specific viewport context, the amount of displayed page items varies between either `7`
(desktop/tablet) or `5` (mobile). The components handles responsive viewport sizing by default.

<Configurator tagName="p-pagination" story={paginationStory} />

