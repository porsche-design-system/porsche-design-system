import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { wordmarkStory } from '@/app/components/wordmark/wordmark.stories';
import Link from 'next/link';
import { Notification } from '@/components/common/Notification';

export const metadata = {
  title: 'Wordmark - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Wordmark component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Wordmark <ComponentStatus tagName="p-wordmark"></ComponentStatus>

The `p-wordmark` gives the Porsche brand a distinctive look, sets it apart from others within the overall external image
and represents the quality of the product.

<Notification heading="Sizing" heading-tag="h2" state="info">
  To preserve backwards compatibility, the `p-wordmark` is bound by its `height` — the `width` follows the wordmark's
  intrinsic aspect ratio automatically.

Setting `--p-wordmark-width` alone will not increase the rendered size, because the height fallback still applies. If
you need to constrain by width, set `--p-wordmark-height: auto` together with `--p-wordmark-width`.

</Notification>

You can use native `click`, `focus`, `focusin`, `blur` and `focusout` events on the `p-wordmark`.

<Configurator tagName="p-wordmark" story={wordmarkStory} />
