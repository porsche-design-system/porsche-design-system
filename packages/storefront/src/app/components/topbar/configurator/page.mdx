import { ComponentStatus } from '@/components/components/ComponentStatus';
import {topbarSlotStories, topbarStory} from "@/app/components/topbar/topbar.stories";
import {Configurator} from "@/components/playground/Configurator";

export const metadata = {
  title: 'Topbar - Porsche Design System',
  description: 'Learn how to use the Porsche Design System\'s Topbar component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.'
}

# Topbar <ComponentStatus tagName="p-topbar"></ComponentStatus>

The `p-topar` component …

<Configurator tagName="p-topbar" story={topbarStory} slotStories={topbarSlotStories} />
