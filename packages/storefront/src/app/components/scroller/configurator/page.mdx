import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { scrollerMeta } from '@/app/components/scroller/scroller.meta';

export const metadata = {
  title: 'Scroller - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Scroller component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Scroller <ComponentStatus tagName="p-scroller"></ComponentStatus>

<scrollerMeta.introduction />

<Configurator tagName="p-scroller" story={scrollerMeta.configurator.story} />
