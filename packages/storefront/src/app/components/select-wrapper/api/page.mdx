import { ComponentStatus } from '@/components/components/ComponentStatus';
import { ComponentApi } from "@/components/components/ComponentApi";

export const metadata = {
  title: 'Select Wrapper - Porsche Design System',
  description: 'Detailed API reference for the Select Wrapper component, including properties, slots, events, and customizable CSS properties.'
}

# Select Wrapper <ComponentStatus tagName="p-select-wrapper"></ComponentStatus>

<ComponentApi tagName="p-select-wrapper" />