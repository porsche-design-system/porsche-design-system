import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { stepperHorizontalMeta } from '@/app/components/stepper-horizontal/stepper-horizontal.meta';

export const metadata = {
  title: 'Stepper Horizontal - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Stepper Horizontal component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Stepper Horizontal <ComponentStatus tagName="p-stepper-horizontal"></ComponentStatus>

<stepperHorizontalMeta.introduction />

<Configurator tagName="p-stepper-horizontal" story={stepperHorizontalMeta.configurator.story} />

