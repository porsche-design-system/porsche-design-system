import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { inputMonthStory, inputMonthSlotStories } from '@/app/components/input-month/input-month.stories';

export const metadata = {
  title: 'Input Month - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Input Month component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Input Month <ComponentStatus tagName="p-input-month"></ComponentStatus>

The `p-input-month` component provides a versatile, user‑friendly interface for entering month values. It supports
direct keyboard input, including arrow-key adjustments for precise changes. It has a start and end slot in order
to support flexible slotted content.

A `label` is a caption which informs the user what information a particular form field is asking for. The
`p-input-month` component can be used with or without a label, but it's recommended to keep the label visible for
better accessibility whenever possible. When used without a label, it's mandatory to provide a descriptive label text
for screen readers.

<Configurator tagName="p-input-month" story={inputMonthStory} slotStories={inputMonthSlotStories} />
