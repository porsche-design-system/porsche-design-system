import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { inlineNotificationMeta } from '@/app/components/inline-notification/inline-notification.meta';

export const metadata = {
  title: 'Inline Notification - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Inline Notification component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Inline Notification <ComponentStatus tagName="p-inline-notification"></ComponentStatus>

<inlineNotificationMeta.introduction />

<Configurator tagName="p-inline-notification" story={inlineNotificationMeta.configurator.story} />
