import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from '@/components/playground/Configurator';
import { inputEmailStory, inputEmailSlotStories } from '@/app/components/input-email/input-email.stories';

export const metadata = {
  title: 'Input Email - Porsche Design System',
  description:
    "Learn how to use the Porsche Design System's Input Email component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.",
};

# Input Email <ComponentStatus tagName="p-input-email"></ComponentStatus>

The `p-input-email` component provides a versatile, user‑friendly interface for entering freeform text. It supports
direct keyboard input and includes `start` and `end` slots to allow flexible integration of icons, buttons, or other content
within the input field.

A `label` is a caption which informs the user what information a particular form field is asking for. The
`p-input-email` component can be used with or without a label, but it's recommended to keep the label visible for
better accessibility whenever possible. When used without a label, it's mandatory to provide a descriptive label text
for screen readers.

While a `placeholder` is optional but recommended to be set whenever bits of example content or hints shall be shown to
give the user visual cues to fill out the form.

<Configurator tagName="p-input-email" story={inputEmailStory} slotStories={inputEmailSlotStories} />
