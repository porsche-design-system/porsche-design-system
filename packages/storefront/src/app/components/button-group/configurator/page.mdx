import { ComponentStatus } from '@/components/components/ComponentStatus';
import { Configurator } from "@/components/playground/Configurator";
import { buttonGroupStory } from "@/app/components/button-group/button-group.stories";

export const metadata = {
  title: 'Button Group - Porsche Design System',
  description: 'Learn how to use the Porsche Design System\'s Button Group component. Explore usage examples, configuration options, and behavior details in a fully controlled setup.'
}

# Button Group <ComponentStatus tagName="p-button-group"></ComponentStatus>

The `p-button-group` is a component for wrapping multiple buttons and display them in vertical or horizontal groups with
corresponding spacing and width. By default buttons are displayed in full-width as a vertical stacked group for mobile
viewports (breakpoint XS) and side-by-side in a row for larger viewports.

<Configurator tagName="p-button-group" story={buttonGroupStory} />