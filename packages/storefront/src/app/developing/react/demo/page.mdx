import { TableOfContents } from '@/components/common/TableOfContents';
import { WebsiteViewer } from '@/components/common/WebsiteViewer';

export const metadata = {
  title: 'React: Demo - Porsche Design System',
  description: 'Start developing in React with the Porsche Design System.',
};

# React

<TableOfContents headings={[]} />

We've created a public GitHub repository with a basic sample project to demonstrate how React is managed in real-world
code. Explore the React example project here:

<WebsiteViewer
  sourceCodePath="frameworks/react"
  viewPath="react"
  title="React: Demo application"
/>
