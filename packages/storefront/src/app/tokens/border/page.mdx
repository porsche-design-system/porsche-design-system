import { TableOfContents } from '@/components/common/TableOfContents';
import {
  borderRadius3Xl,
  borderRadius4Xl,
  borderRadiusXl,
  borderWidthThin,
  borderWidthRegular,
  borderRadius2Xl,
  borderRadiusFull,
  borderRadiusLg,
  borderRadiusMd,
  borderRadiusSm,
  borderRadiusX,
  borderRadiusXs,
} from '@porsche-design-system/tokens';

export const metadata = {
  title: 'Tokens: Border - Porsche Design System',
  description: 'Detailed API reference for the border tokens.',
};

# Border

<TableOfContents headings={[]} />

## Tokens

### Radius

| Token              | Description                         | Value              |
| ------------------ | ----------------------------------- | ------------------ |
| `borderRadiusXs`   | Applies a **Xs** `border-radius`.   | {borderRadiusXs}   |
| `borderRadiusSm`   | Applies a **Sm** `border-radius`.   | {borderRadiusSm}   |
| `borderRadiusMd`   | Applies a **Md** `border-radius`.   | {borderRadiusMd}   |
| `borderRadiusLg`   | Applies a **Lg** `border-radius`.   | {borderRadiusLg}   |
| `borderRadiusXl`   | Applies a **Xl** `border-radius`.   | {borderRadiusXl}   |
| `borderRadius2Xl`  | Applies a **2Xl** `border-radius`.  | {borderRadius2Xl}  |
| `borderRadius3Xl`  | Applies a **3Xl** `border-radius`.  | {borderRadius3Xl}  |
| `borderRadius4Xl`  | Applies a **4Xl** `border-radius`.  | {borderRadius4Xl}  |
| `borderRadiusFull` | Applies a **full** `border-radius`. | {borderRadiusFull} |

### Width

| Token                | Description                           | Value                |
| -------------------- | ------------------------------------- | -------------------- |
| `borderWidthThin`    | Applies a **thin** `border-width`.    | {borderWidthThin}    |
| `borderWidthRegular` | Applies a **regular** `border-width`. | {borderWidthRegular} |
