import { TableOfContents } from '@/components/common/TableOfContents';
import {
  borderRadius3Xl,
  borderRadius4Xl,
  borderRadiusXl,
  borderWidthThin,
  borderWidthRegular,
  borderRadius2Xl,
  borderRadiusFull,
  borderRadiusLg,
  borderRadiusMd,
  borderRadiusSm,
  borderRadiusXs,
} from '@porsche-design-system/tokens';

export const metadata = {
  title: 'Tokens: Border - Porsche Design System',
  description: 'Detailed API reference for the border tokens.',
};

# Border

<TableOfContents headings={[]} />

## Tokens

### Radius

| Token              | Description                       | Value              |
| ------------------ | --------------------------------- | ------------------ |
| `borderRadiusXs`   | Holds a **xs** `border-radius`.   | {borderRadiusXs}   |
| `borderRadiusSm`   | Holds a **sm** `border-radius`.   | {borderRadiusSm}   |
| `borderRadiusMd`   | Holds a **md** `border-radius`.   | {borderRadiusMd}   |
| `borderRadiusLg`   | Holds a **lg** `border-radius`.   | {borderRadiusLg}   |
| `borderRadiusXl`   | Holds a **xl** `border-radius`.   | {borderRadiusXl}   |
| `borderRadius2Xl`  | Holds a **2xl** `border-radius`.  | {borderRadius2Xl}  |
| `borderRadius3Xl`  | Holds a **3xl** `border-radius`.  | {borderRadius3Xl}  |
| `borderRadius4Xl`  | Holds a **4xl** `border-radius`.  | {borderRadius4Xl}  |
| `borderRadiusFull` | Holds a **full** `border-radius`. | {borderRadiusFull} |

### Width

| Token                | Description                         | Value                |
| -------------------- | ----------------------------------- | -------------------- |
| `borderWidthThin`    | Holds a **thin** `border-width`.    | {borderWidthThin}    |
| `borderWidthRegular` | Holds a **regular** `border-width`. | {borderWidthRegular} |
