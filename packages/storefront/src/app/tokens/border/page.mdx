import { TableOfContents } from '@/components/common/TableOfContents';
import {
  radius3Xl,
  radius4Xl,
  radiusXl,
  radius2Xl,
  radiusFull,
  radiusLg,
  radiusMd,
  radiusSm,
  radiusXs,
} from '@porsche-design-system/tokens';

export const metadata = {
  title: 'Tokens: Border - Porsche Design System',
  description: 'Detailed API reference for the border tokens.',
};

# Border

<TableOfContents headings={[]} />

## Tokens

### Radius

| Token        | Description                       | Value        |
| ------------ | --------------------------------- | ------------ |
| `radiusXs`   | Holds a **xs** `border-radius`.   | {radiusXs}   |
| `radiusSm`   | Holds a **sm** `border-radius`.   | {radiusSm}   |
| `radiusMd`   | Holds a **md** `border-radius`.   | {radiusMd}   |
| `radiusLg`   | Holds a **lg** `border-radius`.   | {radiusLg}   |
| `radiusXl`   | Holds a **xl** `border-radius`.   | {radiusXl}   |
| `radius2Xl`  | Holds a **2xl** `border-radius`.  | {radius2Xl}  |
| `radius3Xl`  | Holds a **3xl** `border-radius`.  | {radius3Xl}  |
| `radius4Xl`  | Holds a **4xl** `border-radius`.  | {radius4Xl}  |
| `radiusFull` | Holds a **full** `border-radius`. | {radiusFull} |
