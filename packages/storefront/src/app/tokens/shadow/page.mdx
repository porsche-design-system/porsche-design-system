import { TableOfContents } from '@/components/common/TableOfContents';
import { shadowMedium, shadowLow, shadowHigh } from '@porsche-design-system/tokens';

export const metadata = {
  title: 'Tokens: Shadow - Porsche Design System',
  description: 'Detailed API reference for the shadow tokens.',
};

# Shadow

<TableOfContents headings={[]} />

## Tokens

| Token          | Description                      | Value          |
| -------------- | -------------------------------- | -------------- |
| `shadowHigh`   | Holds a **high** `box-shadow`.   | {shadowHigh}   |
| `shadowMedium` | Holds a **medium** `box-shadow`. | {shadowMedium} |
| `shadowLow`    | Holds a **low** `box-shadow`.    | {shadowLow}    |
