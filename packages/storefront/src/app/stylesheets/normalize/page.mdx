import { TableOfContents } from '@/components/common/TableOfContents';

export const metadata = {
  title: 'Styles: Normalize - Porsche Design System',
  description: 'Learn how to include CSS reset and base typography styles in your project.',
};

# Normalize Styles

<TableOfContents headings={['Setup', 'Theme']} />

## Setup

The Porsche Design System includes recommended **normalize styles** that provide:

- CSS reset styles
- Base typography definitions

These styles are part of the main CSS file:

```css
@import '@porsche-design-system/components-{js|angular|react|vue}';

/* Alternative: If your bundler requires an explicit .css extension, use this path instead */
@import '@porsche-design-system/components-{js|angular|react|vue}/index.css';
```

Alternatively, you can import only the normalize styles:

```css
@import '@porsche-design-system/components-{js|angular|react|vue}/normalize.css';
```
