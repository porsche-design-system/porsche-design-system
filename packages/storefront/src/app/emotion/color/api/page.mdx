import { TableOfContents } from '@/components/common/TableOfContents';
import { StylesTable } from '@/components/common/StylesTable';
import { filterMeta } from '@/lib/filterMeta';
import { emotionMeta } from '@porsche-design-system/emotion-meta';

export const metadata = {
  title: 'Emotion: Color - Porsche Design System',
  description: 'Detailed API reference for the Color style.',
};

# Color

<TableOfContents headings={['JS']} />

## JS

### Background

<StylesTable
  meta={filterMeta(emotionMeta.color.lightDark, (k) => k.startsWith('colorCanvas') || k.startsWith('colorSurface') || k.startsWith('colorFrosted') || k.startsWith('colorBackdrop'))}
  columnLabel="JS"
  showColorSwatch
/>

### Foreground

<StylesTable
  meta={filterMeta(emotionMeta.color.lightDark, (k) => k.startsWith('colorPrimary') || k.startsWith('colorContrast'))}
  columnLabel="JS"
  showColorSwatch
/>

### Semantic

<StylesTable
  meta={filterMeta(emotionMeta.color.lightDark, (k) => k.startsWith('colorInfo') || k.startsWith('colorSuccess') || k.startsWith('colorWarning') || k.startsWith('colorError'))}
  columnLabel="JS"
  showColorSwatch
/>

### A11y

<StylesTable
  meta={filterMeta(emotionMeta.color.lightDark, (k) => k.startsWith('colorFocus'))}
  columnLabel="JS"
  showColorSwatch
/>
