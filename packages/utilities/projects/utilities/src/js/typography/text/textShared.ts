import { fontFamily, fontLineHeight, fontStyle, fontVariant, fontWeight } from '../../font';

export const textFontPartA = `${fontStyle.normal} ${fontVariant} ${fontWeight.regular} `;
export const textFontPartB = `/${fontLineHeight} ${fontFamily}`;
