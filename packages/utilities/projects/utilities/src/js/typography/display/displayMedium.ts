import { fontFamily, fontLineHeight, fontSize, fontStyle, fontVariant, fontWeight } from '../../font';

export const displayMedium = {
  font: `${fontStyle.italic} ${fontVariant} ${fontWeight.semiBold} ${fontSize.fluid.displayMedium}/${fontLineHeight} ${fontFamily}`,
};
