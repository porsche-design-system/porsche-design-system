import { fontFamily, fontLineHeight, fontSize, fontStyle, fontVariant, fontWeight } from '../../font';

export const displayLarge = {
  font: `${fontStyle.italic} ${fontVariant} ${fontWeight.semiBold} ${fontSize.fluid.displayLarge}/${fontLineHeight} ${fontFamily}`,
};
