import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSize } from '../../font';

export const headingXLargeFluid = {
  font: `${headingFontPartA}${fontSize.fluid.headingXLarge}${headingFontPartB}`,
};
