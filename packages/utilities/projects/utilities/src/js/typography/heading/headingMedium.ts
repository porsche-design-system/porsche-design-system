import { fontFamily, fontLineHeight, fontSize, fontStyle, fontVariant, fontWeight } from '../../font';

export const headingMedium = {
  font: `${fontStyle.normal} ${fontVariant} ${fontWeight.semiBold} ${fontSize.fluid.headingMedium}/${fontLineHeight} ${fontFamily}`,
};
