import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSize } from '../../font';

export const headingMediumFluid = {
  font: `${headingFontPartA}${fontSize.fluid.headingMedium}${headingFontPartB}`,
};
