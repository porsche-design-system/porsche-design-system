import { fontFamily, fontLineHeight, fontSize, fontStyle, fontVariant, fontWeight } from '../../font';

export const headingXXXLarge = {
  font: `${fontStyle.normal} ${fontVariant} ${fontWeight.semiBold} ${fontSize.fluid.headingXxxLarge}/${fontLineHeight} ${fontFamily}`,
};
