import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSizeHeadingSmall } from '../../font';

export const headingSmall = {
  font: `${headingFontPartA}${fontSizeHeadingSmall}${headingFontPartB}`,
};
