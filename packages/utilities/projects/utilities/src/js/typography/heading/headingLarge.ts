import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSizeHeadingLarge } from '../../font';

export const headingLarge = {
  font: `${headingFontPartA}${fontSizeHeadingLarge}${headingFontPartB}`,
};
