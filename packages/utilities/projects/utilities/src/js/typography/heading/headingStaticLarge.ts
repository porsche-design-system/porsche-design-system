import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSize } from '../../font';

export const headingStaticLarge = {
  font: `${headingFontPartA}${fontSize.static.headingLarge}${headingFontPartB}`,
};
