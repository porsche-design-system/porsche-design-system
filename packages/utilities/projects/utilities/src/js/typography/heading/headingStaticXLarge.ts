import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSize } from '../../font';

export const headingStaticXLarge = {
  font: `${headingFontPartA}${fontSize.static.headingXLarge}${headingFontPartB}`,
};
