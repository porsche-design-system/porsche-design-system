import { fontFamily, fontLineHeight, fontSize, fontStyle, fontVariant, fontWeight } from '../../font';

export const headingXXLarge = {
  font: `${fontStyle.normal} ${fontVariant} ${fontWeight.semiBold} ${fontSize.fluid.headingXxLarge}/${fontLineHeight} ${fontFamily}`,
};
