import { headingFontPartA, headingFontPartB } from './headingShared';
import { fontSize } from '../../font';

export const headingFluidSmall = {
  font: `${headingFontPartA}${fontSize.fluid.headingSmall}${headingFontPartB}`,
};
