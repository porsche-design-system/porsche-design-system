{
  "name": "@porsche-design-system/tailwind",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scope": "@porsche-design-system",
  "files": ["dist/"],
  "style": "dist/theme.css",
  "sideEffects": false,
  "scripts": {
    "build": "yarn clean:build && mkdir -p dist && sass --load-path=node_modules --no-source-map src/theme.scss dist/theme.css",
    "clean:build": "rm -rf ./dist"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.4"
  },
  "peerDependencies": {
    "sass": "*"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
