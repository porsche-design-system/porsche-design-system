{
  "name": "utilities-v2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "SKIP_PREFLIGHT_CHECK=true react-scripts start",
    "start-app": "yarn start",
    "build": "yarn --cwd ./projects/utilities build && yarn --cwd ./projects/styles build",
    "test": "yarn test:unit && yarn test:vrt",
    "test:unit": "yarn --cwd ./projects/utilities test:unit && yarn --cwd ./projects/styles test:unit",
    "test:vrt": "PORT=3000 BROWSER=none jest --colors -c tests/vrt/config/jest.config.ts",
    "clean": "yarn clean:node_modules && yarn clean:build",
    "clean:node_modules": "rm -rf ./node_modules && rm -rf ./yarn-error.log && yarn --cwd ./projects/utilities clean:node_modules && yarn --cwd ./projects/styles clean:node_modules",
    "clean:build": "yarn --cwd ./projects/utilities clean:build && yarn --cwd ./projects/styles clean:build"
  },
  "dependencies": {
    "clsx": "^1.1.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.3.0",
    "styled-components": "^5.2.3"
  },
  "devDependencies": {
    "@porsche-design-system/shared": "0.0.0",
    "@types/node": "^16.10.3",
    "@types/react": "^17.0.29",
    "@types/react-dom": "^17.0.9",
    "@types/react-router-dom": "^5.3.1",
    "react-scripts": "^4.0.2",
    "sass": "^1.42.0",
    "ts-node": "^10.3.0",
    "typescript": "~4.4.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
