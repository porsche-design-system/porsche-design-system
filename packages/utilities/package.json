{
  "name": "utilities-v2",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "yarn build:ag-grid",
    "build:ag-grid": "yarn --cwd ./projects/ag-grid build",
    "test": "yarn test:unit",
    "test:unit": "yarn test:unit:ag-grid",
    "test:unit:ag-grid": "yarn --cwd ./projects/ag-grid test",
    "clean:build": "yarn --cwd ./projects/utilities clean:build"
  },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
