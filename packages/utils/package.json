{
  "name": "@porsche-ui/utils",
  "scope": "@porsche-ui",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "yarn build:clean && yarn build:component-overview-styles",
    "build:clean": "rm -rf ./dist && mkdir ./dist",
    "build:component-overview-styles": "node component-overview-styles/build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@porsche-ui/ui-kit-scss-utils": "^1.0.0-alpha.1",
    "node-sass-tilde-importer": "^1.0.0",
    "node-sass": "^4.12.0"
  },
  "files": [
    "dist/"
  ]
}
