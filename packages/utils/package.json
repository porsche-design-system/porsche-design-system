{
  "name": "@porsche-ui/utils",
  "scope": "@porsche-ui",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf ./dist && mkdir ./dist && yarn node ./scripts/build.js",
    "clean": "rm -rf ./node_modules && rm -rf ./yarn-error.log && rm -rf ./dist"
  },
  "devDependencies": {
    "@porsche-ui/ui-kit-scss-utils": "^1.0.0-alpha.1",
    "node-sass-tilde-importer": "^1.0.0",
    "node-sass": "^4.12.0"
  },
  "files": [
    "dist/"
  ]
}
