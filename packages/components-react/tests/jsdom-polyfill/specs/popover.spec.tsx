import { PPopover } from '../../../projects/components-wrapper/src';

const Sample = (): JSX.Element => {
  return <PPopover />;
};
describe('Popover', () => {
  it('should', async () => {
    expect(true).toBe(true);
  });
});
