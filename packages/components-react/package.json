{
  "name": "@porsche-design-system/react",
  "version": "0.0.0",
  "private": true,
  "scope": "@porsche-design-system",
  "author": "Dr. Ing. h.c. F. Porsche AG",
  "scripts": {
    "start": "SKIP_PREFLIGHT_CHECK=true concurrently -k -s 'first' -n 'cdn,react' 'serve-cdn' 'react-scripts start'",
    "start-app": "concurrently -k -s 'first' -n 'cdn,app' 'serve-cdn' 'ws --spa index.html -p 3000 -d ./build'",
    "build": "yarn clean:build && yarn build:wrappers && yarn build:jsdom-polyfill && yarn build:uxpin-wrapper",
    "build-app": "SKIP_PREFLIGHT_CHECK=true react-scripts build",
    "build:wrappers": "yarn rollup -c ./projects/components-wrapper/rollup.config.js",
    "build:jsdom-polyfill": "rollup -c ./projects/jsdom-polyfill/rollup.config.js",
    "build:uxpin-wrapper": "yarn --cwd ./projects/uxpin-wrapper build",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "test": "yarn test:jsdom-polyfill && yarn test:unit && yarn test:e2e && yarn test:vrt",
    "test:unit": "jest --colors -c tests/unit/config/jest.config.js --coverage",
    "test:jsdom-polyfill": "concurrently -k -s 'first' -n 'cdn,test' 'serve-cdn' 'jest --colors -c tests/jsdom-polyfill/config/jest.config.js'",
    "test:e2e": "yarn build-app && TYPE=E2E PORT=3000 jest --colors -c tests/e2e/config/jest.config.ts",
    "test:vrt": "if [ \"$CI\" != true ]; then yarn build-app; fi && TYPE=VRT PORT=3000 jest --colors -c tests/vrt/config/jest.config.ts",
    "clean": "yarn clean:node_modules && yarn clean:build",
    "clean:node_modules": "rm -rf ./node_modules && rm -rf ./yarn-error.log && yarn --cwd projects/nextjs clean:node_modules && yarn --cwd projects/uxpin-wrapper clean:node_modules",
    "clean:build": "rm -rf ./build && rm -rf ./dist && yarn --cwd projects/nextjs clean:build && yarn --cwd projects/uxpin-wrapper clean:build"
  },
  "dependencies": {
    "@juggle/resize-observer": "^3.3.1",
    "@porsche-design-system/components-react": "2.12.1",
    "construct-style-sheets-polyfill": "^3.1.0",
    "intersection-observer": "^0.12.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-imask": "^6.4.2",
    "react-router-dom": "^6.3.0",
    "scroll-behavior-polyfill": "^2.0.13",
    "whatwg-fetch": "^3.6.2"
  },
  "devDependencies": {
    "@porsche-design-system/assets": "5.0.1",
    "@porsche-design-system/shared": "0.0.0",
    "@rollup/plugin-commonjs": "^22.0.0",
    "@rollup/plugin-node-resolve": "^13.3.0",
    "@rollup/plugin-typescript": "^8.3.2",
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.2.0",
    "@testing-library/user-event": "^14.2.0",
    "@types/react": "^18.0.9",
    "@types/node": "^17.0.41",
    "@types/react-dom": "^18.0.4",
    "concurrently": "^7.2.0",
    "globby": "^11.0.4",
    "local-web-server": "^5.2.0",
    "react-scripts": "^5.0.1",
    "rollup": "^2.73.0",
    "rollup-plugin-copy": "^3.4.0",
    "rollup-plugin-generate-package-json": "^3.2.0",
    "rollup-plugin-modify": "^3.0.0",
    "rollup-plugin-terser": "^7.0.2",
    "ts-node": "^10.8.1",
    "typescript": "~4.7.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "rules": {
      "jsx-a11y/anchor-is-valid": "off"
    }
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
