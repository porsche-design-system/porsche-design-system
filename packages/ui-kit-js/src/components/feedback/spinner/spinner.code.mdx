import { Example } from "../../../src/components/example/Example"

# Spinner

## Introduction
Basic animated spinner to visualize loading states, e.g. page loading, form validation, etc. The Spinner component sizes are predefined.

### X-small

<Example noTheme={false}>
  {(theme) => {
    return (
      <React.Fragment>
        <p-spinner size="x-small" theme={theme} />
      </React.Fragment>
    )
}}
</Example>

### Small

<Example noTheme={false}>
  {(theme) => {
    return (
      <React.Fragment>
        <p-spinner size="small" theme={theme} />
      </React.Fragment>
    )
}}
</Example>

### Medium

<Example noTheme={false}>
  {(theme) => {
    return (
      <React.Fragment>
        <p-spinner size="medium" theme={theme} />
      </React.Fragment>
    )
}}
</Example>

### Large

<Example noTheme={false}>
  {(theme) => {
    return (
      <React.Fragment>
        <p-spinner size="large" theme={theme} />
      </React.Fragment>
    )
}}
</Example>

