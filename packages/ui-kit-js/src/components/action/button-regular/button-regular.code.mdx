import { Example } from "../../../src/components/example/Example"

# Button Regular

## Introduction
All button types can be generated out of the basic button by implementing additional modifier classes or React proprties.

### Buttons as button elements
Buttons used as `<button>` elements to trigger actions.

### Regular basic (default)
If using basic type, no extra class/prop declaration is needed.

<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular theme={theme}>Click Here!</p-button-regular>
        <p-button-regular theme={theme} disabled="true">Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

### Regular ghost

<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular variant="ghost" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular variant="ghost" theme={theme} disabled="true">Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

### Regular highlight

<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular variant="highlight" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular variant="highlight" theme={theme} disabled="true">Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

### Regular loader state
If a button has a loading state, it must also be set in a disabled state to prevent click behaviour caused by the user.
To show the loader icon, the default svg icon must be replaced with the loader component

<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular loading="true" theme={theme}>Loading...</p-button-regular>
        <p-button-regular variant="ghost" loading="true" theme={theme}>Loading...</p-button-regular>
        <p-button-regular variant="highlight" loading="true" theme={theme}>Loading...</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

---

### Button as link element
Buttons used as link elements (`<a>`), e.g. as a navigation item.

#### Regular basic (default)
<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular href="/lorem/ipsum" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular href="#" disabled="true" theme={theme}>Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

#### Regular ghost
<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular href="/lorem/ipsum" variant="ghost" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular href="#" variant="ghost" disabled theme={theme}>Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

#### Regular highlight
<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular href="/lorem/ipsum" variant="highlight" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular href="#" variant="highlight" disabled theme={theme}>Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

#### Regular loading state
<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular href="/lorem/ipsum" variant="highlight" loading="true" theme={theme}>Loading...</p-button-regular>
        <p-button-regular href="#" variant="ghost" loading="true" theme={theme}>Loading...</p-button-regular>
        <p-button-regular href="#" loading="true" theme={theme}>Loading...</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

---

### Button regular with specific icon
If another icon needs to be implemented, just replace the default icon with your new icon.

<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular icon="icon_phone.min.svg" theme={theme}>Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

### Button regular small
All button types can be rendered in a smaller variant.

#### Variants
<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular small="true" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular small="true" variant="ghost" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular small="true" variant="highlight" theme={theme}>Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

#### Loading state

<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    return (
      <React.Fragment>
        <p-button-regular small="true" loading="true" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular small="true" loading="true" variant="ghost" theme={theme}>Click Here!</p-button-regular>
        <p-button-regular small="true" loading="true" variant="highlight" theme={theme}>Click Here!</p-button-regular>
      </React.Fragment>
    )
}}
</Example>

---

### Button edge cases
Visualization of edge cases

#### Long text
<Example noTheme={false} className="sg-example-button-regular">
  {(theme) => {
    const wrapperStyle = {"width": "200px"}
    return (
      <React.Fragment>
        <div style={wrapperStyle}>
          <p-button-regular icon="icon_phone.min.svg" theme={theme}>Very long text lorem ipsum dolor sit amet consecutor lorem ipsum</p-button-regular>
        </div>
      </React.Fragment>
    )
}}
</Example>
