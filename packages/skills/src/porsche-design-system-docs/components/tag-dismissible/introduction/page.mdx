# Tag Dismissible 

`p-tag-dismissible` is used in contexts where the user can actively remove a tag. It is often seen in filtering.

> **Interactive Configurator**: Try the `p-tag-dismissible` configurator in the live documentation to customize all properties interactively.