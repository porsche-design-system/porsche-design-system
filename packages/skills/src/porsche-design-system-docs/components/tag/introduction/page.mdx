# Tag 

`p-tag` is used to label, categorize, or organize items by using keywords that describe them.

> **Interactive Configurator**: Try the `p-tag` configurator in the live documentation to customize all properties interactively.