{
  "name": "@porsche-design-system/styles-2",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scope": "@porsche-design-system",
  "author": "Dr. Ing. h.c. F. Porsche AG",
  "homepage": "https://designsystem.porsche.com",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "yarn clean:build && yarn build:shared-styles && yarn build:global-styles && yarn build:tailwindcss && yarn build:scss && yarn build:emotion && yarn build:vanilla-extract && yarn build:app",
    "build-app": "tsc -b && vite build",
    "build-prod": "yarn clean:build && yarn build:shared-styles && yarn build-prod:global-styles && yarn build:tailwindcss && yarn build:scss && yarn build:emotion && yarn build:vanilla-extract && yarn build:app",
    "build:app": "vite build",
    "build:shared-styles": "yarn --cwd projects/shared-styles build",
    "build:global-styles": "yarn --cwd projects/global-styles build",
    "build:tailwindcss": "yarn --cwd projects/tailwindcss build",
    "build:scss": "yarn --cwd projects/scss build",
    "build:emotion": "yarn --cwd projects/emotion build",
    "build:vanilla-extract": "yarn --cwd projects/vanilla-extract build",
    "build-prod:global-styles": "yarn --cwd projects/global-styles build-prod",
    "test:unit": "yarn test:unit:global-styles && yarn test:unit:global-styles && yarn test:unit:tailwindcss && yarn test:unit:scss && yarn test:unit:emotion && yarn test:unit:vanilla-extract",
    "test:unit:shared-styles": "yarn --cwd projects/shared-styles test:unit",
    "test:unit:global-styles": "yarn --cwd projects/global-styles test:unit",
    "test:unit:tailwindcss": "yarn --cwd projects/tailwindcss test:unit",
    "test:unit:scss": "yarn --cwd projects/scss test:unit",
    "test:unit:emotion": "yarn --cwd projects/emotion test:unit",
    "test:unit:vanilla-extract": "yarn --cwd projects/vanilla-extract test:unit",
    "clean:build": "yarn clean:build:app && yarn clean:build:shared-styles && yarn clean:build:global-styles && yarn clean:build:tailwindcss && yarn clean:build:scss && yarn clean:build:emotion && yarn clean:build:vanilla-extract",
    "clean:build:app": "rm -rf ./dist",
    "clean:build:shared-styles": "yarn --cwd projects/shared-styles clean:build",
    "clean:build:global-styles": "yarn --cwd projects/global-styles clean:build",
    "clean:build:tailwindcss": "yarn --cwd projects/tailwindcss clean:build",
    "clean:build:scss": "yarn --cwd projects/scss clean:build",
    "clean:build:emotion": "yarn --cwd projects/emotion clean:build",
    "clean:build:vanilla-extract": "yarn --cwd projects/vanilla-extract clean:build"
  },
  "devDependencies": {
    "@porsche-design-system/shared": "0.0.0"
  },
  "peerDependencies": {
    "typescript": "*",
    "@tailwindcss/vite": "*",
    "vite": "*",
    "react": "*",
    "react-dom": "*"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
