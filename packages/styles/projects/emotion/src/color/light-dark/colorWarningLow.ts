import { colorWarningLow as _colorWarningLow } from '@porsche-design-system/tokens';

/** Applies the **warning-low** color, typically used for text or border. */
export const colorWarningLow = `var(--_color-warning-low, ${_colorWarningLow})`;
