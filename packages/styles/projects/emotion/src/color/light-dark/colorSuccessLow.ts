import { colorSuccessLow as _colorSuccessLow } from '@porsche-design-system/tokens';

/** Applies the **success-low** color, typically used for text or border. */
export const colorSuccessLow = `var(--_color-success-low, ${_colorSuccessLow})`;
