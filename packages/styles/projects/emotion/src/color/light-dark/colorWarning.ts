import { colorWarning as _colorWarning } from '@porsche-design-system/tokens';

/** Applies the **warning** color, typically used for text. */
export const colorWarning = `var(--_color-warning, ${_colorWarning})`;
