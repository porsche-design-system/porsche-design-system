import { colorContrastLow as _colorContrastLow } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **contrast-low** color, intended only for decorative elements. */
export const colorContrastLow = `var(--_color-contrast-low, ${_colorContrastLow})`;
