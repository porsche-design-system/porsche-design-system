import { colorInfoFrosted as _colorInfoFrosted } from '@porsche-design-system/tokens';

/** Applies the **info-frosted** color, typically used as background with `.backdrop-blur-frosted`. */
export const colorInfoFrosted = `var(--_color-info-frosted, ${_colorInfoFrosted})`;
