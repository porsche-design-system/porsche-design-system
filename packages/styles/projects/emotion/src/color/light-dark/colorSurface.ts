import { colorSurface as _colorSurface } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **surface** color, typically used for surfaces. */
export const colorSurface = `var(--_color-surface, ${_colorSurface})`;
