import { colorErrorMedium as _colorErrorMedium } from '@porsche-design-system/tokens';

/** Applies the **error-medium** color, typically used for text or border. */
export const colorErrorMedium = `var(--_color-error-medium, ${_colorErrorMedium})`;
