import { colorWarningFrosted as _colorWarningFrosted } from '@porsche-design-system/tokens';

/** Applies the **warning-frosted** color, typically used as background with `.backdrop-blur-frosted`. */
export const colorWarningFrosted = `var(--_color-warning-frosted, ${_colorWarningFrosted})`;
