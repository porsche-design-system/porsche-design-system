import { colorErrorFrostedSoft as _colorErrorFrostedSoft } from '@porsche-design-system/tokens';

/** Applies the **error-frosted-soft** color, typically used as background `:hover`. */
export const colorErrorFrostedSoft = `var(--_color-error-frosted-soft, ${_colorErrorFrostedSoft})`;
