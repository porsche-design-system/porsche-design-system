import { colorCanvas as _colorCanvas } from '@porsche-design-system/tokens';

/** Applies the **canvas** color, typically used for surfaces. */
export const colorCanvas = `var(--_color-canvas, ${_colorCanvas})`;
