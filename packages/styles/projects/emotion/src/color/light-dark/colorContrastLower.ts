import { colorContrastLower as _colorContrastLower } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **contrast-lower** color, intended only for decorative elements. */
export const colorContrastLower = `var(--_color-contrast-lower, ${_colorContrastLower})`;
