import { colorFocus as _colorFocus } from '@porsche-design-system/tokens';

/** Applies the **focus** color, typically used as the outline for `:focus-visible` states. */
export const colorFocus = `var(--_color-focus, ${_colorFocus})`;
