import { _cssVariableGridExtendedSpanOneHalf } from './gridShared';

/** Holds a **half** span within the `extended` area of the Porsche Grid. */
export const gridExtendedSpanOneHalf = `var(${_cssVariableGridExtendedSpanOneHalf})`;
