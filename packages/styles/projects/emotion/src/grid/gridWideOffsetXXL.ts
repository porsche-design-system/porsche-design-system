import { _gridPadding } from './gridSharedOffset';
import { _gridSafeZoneXXL } from './gridShared';

/** Holds a **xxl** offset within the `wide` area of the Porsche Grid. */
export const gridWideOffsetXXL = `calc(${_gridPadding} + ${_gridSafeZoneXXL})`;
