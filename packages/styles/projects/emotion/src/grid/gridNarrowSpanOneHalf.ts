import { _cssVariableGridNarrowSpanOneHalf } from './gridShared';

/** Holds a **one half** span within the `narrow` area of the Porsche Grid. */
export const gridNarrowSpanOneHalf = `var(${_cssVariableGridNarrowSpanOneHalf})`;
