import { _cssVariableGridBasicSpanOneThird } from './gridShared';

/** Holds a **one third** span within the `basic` area of the Porsche Grid. */
export const gridBasicSpanOneThird = `var(${_cssVariableGridBasicSpanOneThird})`;
