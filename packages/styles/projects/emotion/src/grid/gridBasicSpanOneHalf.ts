import { _cssVariableGridBasicSpanOneHalf } from './gridShared';

/** Holds a **half** span within the `basic` area of the Porsche Grid. */
export const gridBasicSpanOneHalf = `var(${_cssVariableGridBasicSpanOneHalf})`;
