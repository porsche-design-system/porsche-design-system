// Public JS API: the documented `vanillaExtractMeta` model, the generated `vanillaExtractDeprecationsMeta`
// catalog of the legacy surface still shipping beside it, their types, and the `kindOf` leaf discriminant.
export { vanillaExtractDeprecationsMeta } from './deprecations';
export { kindOf, type VanillaExtractKind } from './kind';
export { vanillaExtractMeta } from './meta';
export type * from './types';
