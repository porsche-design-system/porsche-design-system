import { fontFamily, fontLineHeight, fontStyleNormal, fontVariant, fontWeightRegular } from '../../font';

export const _headingFontPartA = `${fontStyleNormal} ${fontVariant} ${fontWeightRegular} `;
export const _headingFontPartB = `/${fontLineHeight} ${fontFamily}`;
