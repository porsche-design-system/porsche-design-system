import { colorWarningMedium as _colorWarningMedium } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **warning-medium** color, typically used for text or border. */
export const colorWarningMedium = `var(--_color-warning-medium, ${_colorWarningMedium})`;
