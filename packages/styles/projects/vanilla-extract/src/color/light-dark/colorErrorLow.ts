import { colorErrorLow as _colorErrorLow } from '@porsche-design-system/tokens';

/** Applies the **error-low** color, typically used for text or border. */
export const colorErrorLow = `var(--_color-error-low, ${_colorErrorLow})`;
