import { colorPrimary as _colorPrimary } from '@porsche-design-system/tokens';

/** Applies the **primary** color, typically used for text. */
export const colorPrimary = `var(--_color-primary, ${_colorPrimary})`;
