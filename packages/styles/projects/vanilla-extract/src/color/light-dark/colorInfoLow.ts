import { colorInfoLow as _colorInfoLow } from '@porsche-design-system/tokens';

/** Applies the **info-low** color, typically used for text or border. */
export const colorInfoLow = `var(--_color-info-low, ${_colorInfoLow})`;
