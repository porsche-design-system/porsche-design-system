import { colorBackdrop as _colorBackdrop } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **backdrop** color, typically used for backdrops. */
export const colorBackdrop = `var(--_color-backdrop, ${_colorBackdrop})`;
