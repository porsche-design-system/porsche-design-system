import { colorContrastHigher as _colorContrastHigher } from '@porsche-design-system/tokens';

/** Applies the **contrast-higher** color, typically used for text. */
export const colorContrastHigher = `var(--_color-contrast-higher, ${_colorContrastHigher})`;
