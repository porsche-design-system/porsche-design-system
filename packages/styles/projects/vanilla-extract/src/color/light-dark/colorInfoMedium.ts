import { colorInfoMedium as _colorInfoMedium } from '@porsche-design-system/tokens';

/** Applies the **info-medium** color, typically used for text or border. */
export const colorInfoMedium = `var(--_color-info-medium, ${_colorInfoMedium})`;
