import { colorFrosted as _colorFrosted } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **frosted** color, typically used as a background in combination with `blur()`. */
export const colorFrosted = `var(--_color-frosted, ${_colorFrosted})`;
