import { colorError as _colorError } from '@porsche-design-system/tokens';

/** Applies the **error** color, typically used for text. */
export const colorError = `var(--_color-error, ${_colorError})`;
