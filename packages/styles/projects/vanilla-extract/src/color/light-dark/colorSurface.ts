import { colorSurface as _colorSurface } from '@porsche-design-system/tokens';

/** Applies the **surface** color, typically used for surfaces. */
export const colorSurface = `var(--_color-surface, ${_colorSurface})`;
