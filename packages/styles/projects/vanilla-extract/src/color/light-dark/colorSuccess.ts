import { colorSuccess as _colorSuccess } from '@porsche-design-system/tokens';

/** Holds the [light-dark()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/light-dark) **success** color, typically used for text. */
export const colorSuccess = `var(--_color-success, ${_colorSuccess})`;
