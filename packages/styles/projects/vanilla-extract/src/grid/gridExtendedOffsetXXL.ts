import { _getGridOffsetXXL } from './gridSharedOffset';

/** Holds a **xxl** offset within the `extended` area of the Porsche Grid. */
export const gridExtendedOffsetXXL = _getGridOffsetXXL('extended');
