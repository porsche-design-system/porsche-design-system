import { _cssVariableGridBasicSpanTwoThirds } from './gridShared';

/** Holds a **two thirds** span within the `basic` area of the Porsche Grid. */
export const gridBasicSpanTwoThirds = `var(${_cssVariableGridBasicSpanTwoThirds})`;
