import { shadowLg } from '@porsche-design-system/tokens';

/** @deprecated since v4.0.0, will be removed with next major release. Use boxShadow: shadowLg instead. */
export const dropShadowHighStyle = {
  boxShadow: shadowLg,
} as const;
