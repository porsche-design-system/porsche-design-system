/** Holds the **keyframes** to be used with the `keyframes` vanilla-extract function. */
export const skeletonKeyframes = {
  from: { backgroundPositionX: '100%' },
  to: { backgroundPositionX: '-100%' },
};
