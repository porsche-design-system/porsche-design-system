import { blurFrosted as _blurFrosted } from '@porsche-design-system/tokens';

/** Holds a blur value for a **frosted** effect when combined with a semi-transparent color. */
export const blurFrosted = _blurFrosted;
