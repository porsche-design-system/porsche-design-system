{
  "name": "@porsche-design-system/components-manager-core",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "npm run clean:build && tsc --build tsconfig.json && tsc --project tsconfig.json --module CommonJS --outDir ./dist",
    "test": "npm run test:unit",
    "test:unit": "vitest -c vitest.config.ts",
    "clean:build": "rm -rf ./dist"
  },
  "dependencies": {
    "postcss": "^8.5.16"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "volta": {
    "extends": "../../../../package.json"
  }
}
