{
  "name": "@porsche-design-system/components-manager-core",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "yarn clean:build && tsc --build tsconfig.json",
    "test": "yarn test:unit",
    "test:unit": "jest --coverage",
    "test:unit:watch": "yarn test --watch",
    "clean": "yarn clean:node_modules && yarn clean:build",
    "clean:node_modules": "rm -rf ./node_modules && rm -rf ./yarn-error.log",
    "clean:build": "rm -rf ./dist"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "tslib": "^2.3.0",
    "typescript": "^4.6.2"
  }
}
